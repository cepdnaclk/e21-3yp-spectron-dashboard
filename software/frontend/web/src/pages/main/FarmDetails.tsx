// Compatibility entry point for older deep links and tests. Farm editing now
// lives in the focused FarmSettings screen; the daily farm view is FarmOverview.
export { default } from './FarmSettings';
