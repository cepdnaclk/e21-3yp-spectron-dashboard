# Spectron shared database settings (local development)
DB_HOST=localhost
DB_PORT=5432
DB_USER=spectron
DB_PASSWORD=spectron
DB_NAME=spectron
DATABASE_URL=postgres://spectron:spectron@localhost:5432/spectron?sslmode=disable
